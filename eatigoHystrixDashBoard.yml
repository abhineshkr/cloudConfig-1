spring:
   application:
    name: eatigoHystrixDashBoard    
server:
  port: 10090
  
logging:
  level:
    root: INFO
  
eureka:
  instance:
     hostname: 202.149.218.74
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:10089/eureka/

